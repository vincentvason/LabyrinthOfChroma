using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyBulletAnimation : MonoBehaviour
{
    [SerializeField] private GameObject bulletPrefab;

    [SerializeField] private Vector3 postion;
    [SerializeField] private Vector3 positonChange;
    [SerializeField] private GameObject rotation;
    [SerializeField] private GameObject rotationChange;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
